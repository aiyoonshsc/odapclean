# 개발 로그

## 2024년 7월 26일

- **UI/UX 개편 (Khan Academy 스타일 적용)**
  - `index.css`: 전역 스타일(폰트, 색상 팔레트, 기본 레이아웃) 재정의
  - `App.css`, `MainPage.css`, `Sidebar.css`: 각 컴포넌트별 상세 스타일 추가
  - `App.tsx`: 2단 레이아웃(사이드바 + 메인 콘텐츠) 구조로 변경. 폴더 관련 상태 및 로직을 중앙에서 관리하도록 리팩토링.
  - `Sidebar.tsx`: 폴더 목록 및 생성 UI를 담당하는 사이드바 컴포넌트 신규 생성.
  - `MainPage.tsx`: 문제 생성 및 목록 표시에만 집중하도록 리팩토링. 폴더 관련 로직 제거 및 props 전달 방식으로 변경.
  - `types/index.ts`: `Hint` 타입에 `content` 속성 추가하여 타입 오류 해결.

- **프론트엔드**
  - 폴더 목록 표시 기능 구현
  - 폴더 생성 기능 구현 (낙관적 업데이트 포함)
  - 문제 목록 표시 기능 구현
  - 문제 생성 기능 구현 (API 연동)
  - 힌트 입력 UI 및 로직 추가
  - 문제 목록에 힌트 표시 기능 추가
  - 다수의 Linter 오류 수정 및 코드 안정화

- **백엔드**
  - `user_id` 및 `created_at` 필드를 `Folder` 및 `Problem` 모델에 다시 추가
  - `create_folder` API 수정 (`user_id` 처리)
  - `create_problem` API 수정 (`content` 및 `hints` 필드 처리)
  - `get_problems` API 수정 (`ProblemWithHints` 반환 타입 일치 및 힌트 조회 로직 추가)
  - `Hint` 및 `HintCreate` 모델 추가
  - 다수의 `NameError`, `SyntaxError`, `ValidationError` 해결 및 서버 안정화